<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Update Artwork</title>
  </head>
  <body>
    <h1> Update Artwork: </h1>


    <%= form_tag artwork_path(@artwork), method: "put" do %>
      <label> Edit Title </label>
      <%= text_field_tag :title, @artwork.title %>
      <label> Edit Price </label>
      <%= number_field_tag :price, @artwork.price %>
      <label> Edit Sold </label>
      <%= check_box_tag :sold, @artwork.sold %>
      <%= submit_tag "byee" %>
    <% end %>
    <!-- <%= bootstrap_form_for(@artwork) do |form| %>
      <%= form.text_field :title %>
      <%= form.number_field :price %>
      <%= form.check_box :sold %>
      <%= form.submit "Update Artwork" %>
    <% end %>
 -->



  </body>
</html>
